[tool.poetry]
name = "raspberrypi-pio-poc"
version = "0.14"
description = "Proof of Concept"
authors = ["Nathan Young <nathan@::0/128>"]
license = "Apache-2.0"

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]
pylint = "^2.7.2"
pytest = "^6.2.2"
black = "^21.5b0"
jupyter = "^1.0.0"
matplotlib = "^3.3.4"
seaborn = "^0.11.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
